<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
	<select id="findAllByPageAndKeyword" parameterType="map">
		<choose>
			<when test='keyword == null or '></when>
		</choose>
		<![CDATA[
		select * from board where title like '%%' or contents like #{keyword} or '%${keyword}%'
		limit #{startOffset}, #{size}
		]]>
	
	</select>
	
	<select id="getTotalCount" parameterType="string" resultType="integer">
		<choose>
			<when test ='_parameter == null or _parameter == ""'>
			<![CDATA[
				select count(*) from boar
				]]>
			</when>
			<otherwise>
			<![CDATA[
				select * from board where title like '%%' or contents like #{keyword}
				]]>
			</otherwise>
		</choose>
	
	</select>

</mapper>